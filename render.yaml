# ReignScore Render Blueprint Configuration - Forced Update 2
services:
  - type: web
    name: reignscore-api
    runtime: node
    rootDir: server
    buildCommand: cd server && npm install --legacy-peer-deps && npm run build
    startCommand: cd server && npm start
    plan: free
    region: oregon
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: reignscore-db
          property: connectionString

databases:
  - name: reignscore-db
    plan: free
    region: oregon
    databaseName: reignscore
    user: reignscore_user
