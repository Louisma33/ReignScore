# ReignScore Render Blueprint Configuration - Forced Update 2
services:
  - type: web
    name: reignscore-api-final-v4
    runtime: node
    rootDir: server
    buildCommand: npm install --legacy-peer-deps && npm run build
    startCommand: npm start
    plan: free
    region: oregon
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: reignscore-db
          property: connectionString
      - key: STRIPE_SECRET_KEY
        value: mk_1SviRQKp2Fwwk4U0uGIZMCjO

databases:
  - name: reignscore-db
    plan: free
    region: oregon
    databaseName: reignscore
    user: reignscore_user

  - type: static
    name: reignscore-web
    runtime: static
    buildCommand: npm install && npm run build
    publishDir: dist
    plan: free
    region: oregon
